# SmartSpend v2 — Use Cases

> Все пользовательские сценарии продукта. Основа: PRODUCT_SPEC.md.

---

## Акторы

| Актор | Описание |
|---|---|
| **Гость** | Неаутентифицированный пользователь |
| **Пользователь** | Аутентифицированный пользователь с активным аккаунтом |
| **Автор** | Пользователь, создающий контент (статьи, наборы) |
| **Администратор** | Сотрудник SmartSpend, управляет официальными наборами |
| **Система** | Фоновые процессы: Scheduler, триггеры БД, SSE |

---

## Блок 1 — Аутентификация (UC-01…05)

---

### UC-01 | Регистрация по email

```
Актор:        Гость
Предусловие:  Пользователь не имеет аккаунта
Триггер:      Нажатие "Начать бесплатно" на лендинге
```

**Основной сценарий:**
1. Открывается AuthModal → вкладка "Регистрация"
2. Вводит имя, email, пароль
3. POST /api/auth/register → 201: `{ user, access_token }` + письмо с подтверждением
4. Редирект на `/onboarding` — квиз (3 вопроса о потреблении + имя)
5. POST /api/me/onboarding `{ answers, display_name }`
6. Редирект на `/feed`
7. Баннер: "Подтвердите email" (не блокирует доступ)

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Email уже зарегистрирован | Ошибка "Пользователь уже существует" |
| A2 | Пароль не соответствует требованиям | Инлайн-валидация до отправки |
| A3 | Сервер недоступен | Сообщение об ошибке, форма остаётся заполненной |

**Постусловие:**
- Аккаунт создан, статус `unverified`
- Созданы дефолтные группы инвентаря (Питание, Уход, Одежда, Быт, Здоровье, Разное)
- Access token выдан, refresh token установлен в httpOnly cookie

---

### UC-02 | Вход в аккаунт

```
Актор:        Гость
Предусловие:  Аккаунт существует
Триггер:      Нажатие "Войти" на лендинге или любой защищённой странице
```

**Основной сценарий:**
1. Открывается AuthModal → вкладка "Вход"
2. Вводит email + пароль
3. POST /api/auth/login → 200: `{ user, access_token }`
4. Редирект на `/feed`

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Неверный пароль | Ошибка "Неверный email или пароль" (не раскрывать что именно) |
| A2 | Аккаунт `suspended` | "Аккаунт временно заблокирован. Обратитесь в поддержку" |
| A3 | "Запомнить меня" включён | Refresh token живёт 30 дней вместо сессии |
| A4 | Access token истёк во время сессии | Система автоматически обновляет через POST /api/auth/refresh |

**Постусловие:** Пользователь аутентифицирован, получает доступ ко всем разделам

---

### UC-03 | Вход через OAuth (Яндекс / VK)

```
Актор:        Гость
Предусловие:  —
Триггер:      Нажатие "Войти через Яндекс" или "Войти через VK"
```

**Основной сценарий:**
1. Редирект на страницу авторизации OAuth-провайдера
2. Пользователь подтверждает доступ
3. Провайдер возвращает код → POST /api/auth/oauth/callback
4. Сервер находит или создаёт пользователя по email из провайдера
5. Если **новый пользователь** → редирект на `/onboarding` (квиз обязателен)
6. Если **существующий** → редирект на `/feed`

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Пользователь отклонил доступ в OAuth | Возврат на лендинг без авторизации |
| A2 | Email из OAuth уже зарегистрирован через пароль | Аккаунты связываются, вход выполнен |

**Постусловие:** Аккаунт создан или найден, `social_yandex_linked` / `social_vk_linked` = true

---

### UC-04 | Сброс пароля

```
Актор:        Гость
Предусловие:  Аккаунт с email существует
Триггер:      Нажатие "Забыли пароль?" в AuthModal
```

**Основной сценарий:**
1. Вводит email → POST /api/auth/forgot-password
2. Получает письмо со ссылкой (токен, TTL 1 час)
3. Переходит по ссылке → форма нового пароля
4. Вводит новый пароль → POST /api/auth/reset-password `{ token, password }`
5. Все refresh токены инвалидируются
6. Редирект на AuthModal с сообщением "Пароль изменён, войдите снова"

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Email не найден | Ответ всё равно "Письмо отправлено" (безопасность) |
| A2 | Токен истёк (> 1 ч) | "Ссылка устарела, запросите новую" |
| A3 | Токен уже использован | "Ссылка недействительна" |

**Постусловие:** Пароль изменён, все сессии завершены

---

### UC-05 | Удаление аккаунта

```
Актор:        Пользователь
Предусловие:  Пользователь авторизован
Триггер:      Кнопка "Удалить аккаунт" в Settings → Account
```

**Основной сценарий:**
1. Показывается модал подтверждения с предупреждением
2. Пользователь вводит пароль для подтверждения
3. DELETE /api/me → статус аккаунта → `deleted`
4. Email и personal data анонимизируются немедленно
5. Все публикации остаются, но автор → `ghost` запись
6. Подписки, лайки, инвентарь, финансовые данные удаляются
7. Сессия завершается, редирект на лендинг

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Пользователь отменил | Модал закрывается, аккаунт цел |
| A2 | Неверный пароль | Ошибка "Неверный пароль" |

**Постусловие:**
- Аккаунт в статусе `deleted`, через 30 дней — автоматическая полная очистка (GDPR job)
- Публикации отображаются с автором "👻 Привидение"

---

## Блок 2 — Профиль пользователя (UC-06…07)

---

### UC-06 | Редактирование профиля

```
Актор:        Пользователь
Предусловие:  Пользователь авторизован, открыл Account
Триггер:      Нажатие "Редактировать" в разделе профиля
```

**Основной сценарий:**
1. Пользователь видит форму: имя, handle (@username), bio, аватар
2. Редактирует поля
3. Загружает аватар (drag-and-drop или клик) → POST /api/me/avatar (multipart)
4. Сохраняет → PUT /api/me
5. Данные обновляются во всех местах (лента, профиль автора, комментарии)

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Handle уже занят | Ошибка "Этот @username уже используется" |
| A2 | Файл аватара > лимита | Ошибка "Файл слишком большой (макс. 5 МБ)" |
| A3 | Смена email | Требует подтверждения нового email → POST /api/auth/change-email |
| A4 | Смена пароля | Требует текущий пароль → POST /api/auth/change-password |

**Постусловие:** Профиль обновлён, аватар загружен на S3

---

### UC-07 | Изменение настроек

```
Актор:        Пользователь
Предусловие:  Пользователь авторизован
Триггер:      Переход в Settings
```

**Основной сценарий:**
1. Пользователь видит секции: Внешний вид, Уведомления, Приватность, Интеграции
2. Переключает тему (light/dark/system) → немедленно применяется, PUT /api/me/settings
3. Настраивает уведомления: включает/отключает типы
4. Управляет приватностью: кто видит наборы/статьи/профиль
5. Управляет OAuth-связками: привязать/отвязать Яндекс, VK

**Постусловие:** Настройки сохранены в `user_settings`, применяются мгновенно

---

## Блок 3 — Каталог наборов (UC-08…10)

---

### UC-08 | Просмотр и фильтрация каталога

```
Актор:        Пользователь (Гость — только просмотр)
Предусловие:  —
Триггер:      Переход в раздел Каталог
```

**Основной сценарий:**
1. GET /api/catalog → список наборов с пагинацией
2. Пользователь применяет фильтры: категория, источник (SS / Community / Мои), тип (база / дополнение)
3. Меняет сортировку: новые / популярные за день/неделю/всё время
4. Карточка набора: название, описание, категория, количество позиций, лайки, сколько используют

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Нет наборов по фильтру | Пустое состояние "Наборов не найдено" |
| A2 | Подгрузка следующей страницы | Infinite scroll / кнопка "Загрузить ещё" |

**Постусловие:** —

---

### UC-09 | Просмотр деталей набора

```
Актор:        Пользователь
Предусловие:  Набор существует и опубликован
Триггер:      Клик на карточку набора в каталоге или ленте
```

**Основной сценарий:**
1. GET /api/catalog/:id → данные набора со всеми позициями
2. Просматривает позиции: название, количество, единица, цена, срок/расход
3. Может настроить масштаб (Scale 0.25x – 5x) для пересчёта qty и цен
4. Нажимает лайк → POST /api/sets/:id/like (оптимистичное обновление)
5. Видит автора набора → может перейти на его профиль

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Набор уже добавлен в конверт | Кнопка "Добавить" → "Уже добавлен" |
| A2 | Автор — анонимный | Показывается "🔒 Анонимный автор", переход заблокирован |
| A3 | Автор — удалённый аккаунт | Показывается "👻 Привидение" |

**Постусловие:** Счётчик просмотров набора увеличен (через очередь, flush раз в 10 мин)

---

### UC-10 | Добавление набора в конверт

```
Актор:        Пользователь
Предусловие:  Пользователь авторизован, просматривает SetDetail
Триггер:      Нажатие "Добавить в конверт"
```

**Основной сценарий:**
1. POST /api/sets/:id/save
2. Сервер создаёт `envelope` для категории набора (если не существует)
3. Добавляет набор в `envelope_sets`
4. Для каждой позиции набора создаёт `inventory_item` со статусом `frozen`
5. Создаётся уведомление `inv_frozen`: "Добавлено N позиций в заморозку"
6. Редирект на `/inventory`, раздел "Заморозка"

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Набор уже добавлен | Ошибка или диалог "Добавить повторно?" |
| A2 | Пользователь отредактировал qty/price перед добавлением | Сохранённые значения используются при создании `inventory_item` |

**Постусловие:**
- Envelope создан/обновлён
- Все позиции набора в инвентаре со статусом `frozen`
- `set_saves` запись создана, счётчик `users_count` набора +1

---

## Блок 4 — Инвентарь (UC-11…16)

---

### UC-11 | Просмотр инвентаря

```
Актор:        Пользователь
Предусловие:  Пользователь авторизован
Триггер:      Переход в раздел Инвентарь
```

**Основной сценарий:**
1. GET /api/inventory → группы с позициями
2. Система вычисляет статус каждой позиции на лету (не из БД):
   - `consumable`: `pct = qty / (default_qty)` → ok / soon / urgent / over
   - `wear`: `pct = 1 - days_used / total_days` → ok / soon / urgent / over
3. Отображаются кольца прогресса с цветовой кодировкой
4. Раздел "Заморозка" показывается если есть `frozen` позиции (серые, ❄️)

**Постусловие:** —

---

### UC-12 | Активация позиции из заморозки (frozen → active)

```
Актор:        Пользователь
Предусловие:  Позиция в статусе `frozen`
Триггер:      Нажатие "Купил" у позиции в заморозке
```

**Основной сценарий:**
1. Открывается форма: подтвердить/изменить qty, цену, дату покупки
2. PUT /api/inventory/items/:id/activate `{ purchase_date: today, qty, price }`
3. Статус → `active`, начинается расчёт сроков
4. Позиция перемещается из "Заморозки" в соответствующую группу
5. Если это была последняя frozen позиция набора → уведомление "Все позиции активированы ✅"

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Пользователь изменил qty | Расчёт идёт от нового значения |
| A2 | Пользователь указал прошедшую дату | Статус может сразу стать `soon` или `urgent` |

**Постусловие:** Позиция активна, участвует в расчёте финансового профиля

---

### UC-13 | Пополнение расходника

```
Актор:        Пользователь
Предусловие:  Позиция типа `consumable` в статусе `active`
Триггер:      Нажатие "Пополнить" у позиции в инвентаре
```

**Основной сценарий:**
1. Открывается форма: сколько добавить + новая цена (опционально)
2. POST /api/inventory/items/:id/restock `{ qty_added, price }`
3. `qty = qty_old + qty_added`, `purchase_date = today`
4. Статус пересчитывается → как правило возвращается в `ok`

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | "Купил заново" (сброс) | PUT /api/inventory/items/:id/activate `{ qty: default, purchase_date: today }` |

**Постусловие:** Количество обновлено, статус пересчитан

---

### UC-14 | Ручное добавление позиции в инвентарь

```
Актор:        Пользователь
Предусловие:  Пользователь авторизован
Триггер:      Нажатие "Добавить вручную" в инвентаре
```

**Основной сценарий:**
1. Открывается форма: название, тип (consumable / wear), группа
2. Для consumable: qty, unit, daily_use, price, purchase_date
3. Для wear: wear_life_weeks, price, purchase_date
4. POST /api/inventory/items `{ ..., is_manual: true }`
5. Позиция сразу в статусе `active` (не frozen, т.к. вручную)

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Группа не существует | Создать новую группу или выбрать "Разное" |

**Постусловие:** Позиция создана с `is_manual=true`, участвует в расчётах

---

### UC-15 | Архивация позиции

```
Актор:        Пользователь
Предусловие:  Позиция в статусе `active`
Триггер:      Нажатие "Архивировать" у позиции
```

**Основной сценарий:**
1. PUT /api/inventory/items/:id/archive
2. Статус → `archived`
3. Позиция скрывается из основного списка (доступна через фильтр "Архив")
4. Исключается из расчёта финансового профиля

**Постусловие:** Позиция в архиве, не влияет на SmartBase

---

### UC-16 | Система напоминает о заканчивающейся позиции

```
Актор:        Система (Scheduler)
Предусловие:  Есть позиции типа `consumable` или `wear` в статусе `active`
Триггер:      Ежедневный job в 09:00
```

**Основной сценарий:**
1. Для каждого пользователя пересчитываются статусы всех активных позиций
2. Если статус изменился: `ok/soon → urgent`:
   - Создаётся notification `inv_reminder`: "Протеин заканчивается — осталось 2 дня"
3. Если статус → `over`:
   - Создаётся notification `inv_reminder` (высокий приоритет): "Зубная паста закончилась"
4. Пользователь видит уведомление в колоколе

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Уведомления типа `inv_reminder` отключены | Job пропускает пользователя |
| A2 | Статус не изменился (уже был urgent) | Уведомление не дублируется |

**Постусловие:** Уведомление создано, доставляется через SSE или polling

---

## Блок 5 — Финансовый профиль (UC-17…19)

---

### UC-17 | Заполнение финансовых данных

```
Актор:        Пользователь
Предусловие:  Пользователь авторизован, ещё не заполнял финансы
Триггер:      Первый переход в Profile или баннер "Заполните финансовые данные"
```

**Основной сценарий:**
1. Открывается FinancialModal
2. Пользователь вводит:
   - Ежемесячный доход
   - Фиксированные расходы: жильё, кредит, еда, транспорт, прочее
   - Текущий капитал
   - Ставка ядра (%)
   - Ставка EmoSpend (4–10%)
3. PUT /api/profile/finance
4. Система моментально считает и показывает:
   - Smart-базу (расходы на все наборы в конвертах)
   - Свободный остаток
   - Отчисление в ядро
   - EmoSpend
   - График прогноза капитала на 10 лет

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Нет активных наборов в конвертах | Smart-база = 0, расчёт всё равно работает |
| A2 | Фиксированные расходы > дохода | Свободный остаток < 0, предупреждение |

**Постусловие:** `user_finance` заполнен, расчёты доступны

---

### UC-18 | Изменение ставки EmoSpend

```
Актор:        Пользователь
Предусловие:  Финансовые данные заполнены
Триггер:      Движение ползунка EmoSpend в Profile
```

**Основной сценарий:**
1. Пользователь двигает ползунок (4%–10%)
2. EmoSpend обновляется в реальном времени (локально)
3. График прогноза пересчитывается мгновенно
4. При отпускании ползунка → PUT /api/profile/finance `{ emo_rate }` (debounce)

**Постусловие:** Новая ставка сохранена

---

### UC-19 | Автоматический пересчёт SmartBase

```
Актор:        Система
Предусловие:  У пользователя есть активные наборы в конвертах
Триггер:      Добавление/удаление набора, активация/архивация позиции инвентаря
```

**Основной сценарий:**
1. После любого изменения в конвертах или инвентаре сервер пересчитывает Smart-базу:
   ```
   consumable: price * (daily_use * 30) / qty
   wear:       price / (wear_life_weeks * 7 / 30)
   ```
2. Возвращается новая сводка GET /api/profile/summary
3. Frontend обновляет цифры в Profile без перезагрузки

**Постусловие:** Финансовые расчёты актуальны

---

## Блок 6 — Лента (UC-20…21)

---

### UC-20 | Просмотр ленты

```
Актор:        Пользователь
Предусловие:  Пользователь авторизован
Триггер:      Переход в раздел Feed
```

**Основной сценарий:**
1. GET /api/feed → смешанный список: статьи + наборы
2. Алгоритм: подписки → популярное из интересных категорий → новые SS наборы → рекомендации
3. Пользователь видит карточки с автором, категорией, превью, счётчиками
4. Прокрутка → подгрузка следующих элементов (cursor-based pagination)

**Постусловие:** Прочитанные элементы отмечаются через POST /api/articles/:id/read

---

### UC-21 | Фильтрация ленты

```
Актор:        Пользователь
Предусловие:  Пользователь на странице Feed
Триггер:      Взаимодействие с фильтрами
```

**Основной сценарий:**
1. Пользователь выбирает тип: Все / Статьи / Наборы
2. Выбирает режим: Все / Подписки / Непрочитанное / Понравившееся
3. Выбирает категорию
4. Меняет сортировку: новые / популярные за день/неделю/всё время
5. GET /api/feed?type=&mode=&cat=&sort= → обновлённый список

**Постусловие:** —

---

## Блок 7 — Статьи (UC-22…26)

---

### UC-22 | Чтение статьи

```
Актор:        Пользователь
Предусловие:  Статья опубликована
Триггер:      Клик на карточку статьи в ленте или каталоге
```

**Основной сценарий:**
1. GET /api/articles/:id → статья со всеми данными
2. Пользователь читает статью (markdown рендерится в HTML)
3. Видит привязанный набор (если есть) → может сразу добавить в конверт
4. POST /api/articles/:id/read → статья помечена прочитанной
5. Лайк → POST /api/articles/:id/like (оптимистичное обновление)

**Постусловие:** `article_reads` запись создана, `views_count` +1 (через очередь)

---

### UC-23 | Написание и публикация статьи

```
Актор:        Автор (Пользователь)
Предусловие:  Пользователь авторизован
Триггер:      Нажатие "Создать статью"
```

**Основной сценарий:**
1. POST /api/articles → создаётся черновик с пустыми полями
2. Автор заполняет: заголовок, лид, тело статьи (markdown-редактор с тулбаром)
3. Загружает изображения через drag-and-drop:
   - POST /api/articles/:id/images (multipart/form-data)
   - Получает S3 URL, вставляет `![alt](url)` в текст
4. Выбирает категорию
5. Опционально: привязывает набор (LinkedSet)
6. Автосохранение черновика каждые 30 сек: PUT /api/articles/:id (status=draft)
7. Предпросмотр: переключает режим, видит статью как читатель
8. Публикует: POST /api/articles/:id/publish
9. Статья попадает в ленту подписчиков
10. Все подписчики получают уведомление `sub_new_article`

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Пустой заголовок | Публикация заблокирована, инлайн-ошибка |
| A2 | Файл изображения слишком большой | Ошибка, изображение не загружается |
| A3 | Автор нажал "Анонимно" | Публикуется с is_anonymous=true (см. UC-26) |
| A4 | Автор закрыл страницу без публикации | Черновик сохранён, доступен в Account |

**Постусловие:** Статья опубликована, `articles_count` автора +1

---

### UC-24 | Редактирование статьи

```
Актор:        Автор
Предусловие:  Статья существует (черновик или опубликована)
Триггер:      Нажатие "Редактировать" в Account или на странице статьи
```

**Основной сценарий:**
1. Открывается редактор с текущим содержимым
2. Автор вносит изменения
3. PUT /api/articles/:id → сохранение
4. Если статья уже опубликована — изменения видны читателям сразу

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Удаление изображения | DELETE /api/articles/images/:id → файл удаляется из S3 |

**Постусловие:** Статья обновлена

---

### UC-25 | Комментирование статьи

```
Актор:        Пользователь
Предусловие:  Статья опубликована, пользователь авторизован
Триггер:      Ввод текста в поле комментария
```

**Основной сценарий:**
1. Пользователь вводит текст → POST /api/articles/:id/comments
2. Комментарий появляется в списке
3. Автор статьи получает уведомление `comment_on_my`
4. Другой пользователь отвечает на комментарий (parent_id) → автор оригинала получает `comment_reply`
5. Лайк комментария → POST /api/comments/:id/like

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Редактирование своего комментария | PUT /api/comments/:id |
| A2 | Удаление своего комментария | DELETE /api/comments/:id → `is_deleted=true`, текст → "[удалено]" |

**Постусловие:** `comments_count` статьи +1

---

### UC-26 | Анонимная публикация

```
Актор:        Автор
Предусловие:  Создаёт статью или набор
Триггер:      Включение toggle "Опубликовать анонимно"
```

**Основной сценарий:**
1. При публикации установлен флаг `is_anonymous=true`
2. В ленте других пользователей: "🔒 Анонимный автор"
3. Клик по автору → заблокирован (AnonymousProfile)
4. В аккаунте автора — статья помечена "Анонимно", автор видит её как свою
5. Автор в любой момент раскрывает себя:
   - PUT /api/articles/:id `{ is_anonymous: false }`
   - Имя автора начинает отображаться у всех

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Другой пользователь пытается узнать автора через API | API никогда не возвращает `author_id` для анонимных публикаций |

**Постусловие:** Реальный `author_id` хранится в БД, но скрыт от клиентов

---

## Блок 8 — Наборы (создание) (UC-27…28)

---

### UC-27 | Создание собственного набора

```
Актор:        Автор
Предусловие:  Пользователь авторизован
Триггер:      Нажатие "Создать набор"
```

**Основной сценарий:**
1. POST /api/sets → черновик создаётся
2. Автор заполняет: заголовок, краткое описание, развёрнутое описание
3. Выбирает категорию (влияет на группу в инвентаре)
4. Выбирает тип: база / дополнение
5. Добавляет позиции: название, ед. изм., цена, qty, срок/расход
6. Выбирает видимость: публичный / приватный
7. Предпросмотр в режиме каталога
8. Публикует: POST /api/sets/:id/publish

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Приватный набор | `source=own`, виден только автору в каталоге |
| A2 | Публичный набор | `source=community`, появляется в каталоге для всех |
| A3 | Анонимная публикация | `is_anonymous=true` (см. UC-26) |
| A4 | Добавление своего набора в конверт | Позиции сразу `frozen`, далее → UC-12 |

**Постусловие:** Набор опубликован, `sets_count` автора +1

---

### UC-28 | Редактирование набора

```
Актор:        Автор
Предусловие:  Набор существует и принадлежит автору
Триггер:      Нажатие "Редактировать" на странице набора или в Account
```

**Основной сценарий:**
1. Открывается форма с текущими данными набора
2. Автор изменяет поля и/или позиции
3. PUT /api/sets/:id → сохранение
4. Если набор уже используют другие — изменения видны им при следующем открытии

**Постусловие:** Набор обновлён

---

## Блок 9 — Подписки и социальное (UC-29…31)

---

### UC-29 | Подписка на автора

```
Актор:        Пользователь
Предусловие:  Оба пользователя существуют, не подписан
Триггер:      Нажатие "Подписаться" на профиле автора или в статье/наборе
```

**Основной сценарий:**
1. POST /api/users/:id/follow
2. Автор получает уведомление `new_follower`: "На вас подписался @username"
3. Контент автора появляется в ленте подписчика (приоритетно)
4. В Feed → режим "Подписки" — только контент от подписок

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Пользователь уже подписан | Кнопка "Подписаться" → "Вы подписаны", повторный клик — отписка |
| A2 | Отписка | DELETE /api/users/:id/follow, контент автора уходит из приоритета |
| A3 | Профиль автора приватный | Кнопка подписки недоступна |

**Постусловие:** `follows` запись создана, `followers_count` автора +1

---

### UC-30 | Просмотр публичного профиля автора

```
Актор:        Пользователь
Предусловие:  Профиль автора публичный, аккаунт активен
Триггер:      Клик на имя автора в ленте, статье или наборе
```

**Основной сценарий:**
1. GET /api/users/:id → публичный профиль
2. Пользователь видит: аватар, имя, bio, статистику (подписчики, статьи, наборы)
3. Список статей автора: GET /api/users/:id/articles
4. Список наборов автора: GET /api/users/:id/sets
5. Может подписаться → UC-29

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Автор удалил аккаунт (`ghost`) | GhostProfile: "Этот аккаунт был удалён", контент остаётся |
| A2 | Автор анонимный | AnonymousProfile: переход заблокирован |

**Постусловие:** —

---

### UC-31 | Лайк набора или статьи

```
Актор:        Пользователь
Предусловие:  Пользователь авторизован, контент существует
Триггер:      Нажатие иконки ❤️
```

**Основной сценарий:**
1. Оптимистичное обновление: счётчик +1 немедленно
2. POST /api/sets/:id/like или POST /api/articles/:id/like
3. При достижении порога (50, 100, 500 лайков) — автор получает `like_milestone`

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Лайк уже стоит — убрать | DELETE /api/sets/:id/like, счётчик -1 |
| A2 | Сервер вернул ошибку | Откат оптимистичного обновления |

**Постусловие:** `set_likes` / `article_likes` запись создана

---

## Блок 10 — Уведомления (UC-32…35)

---

### UC-32 | Просмотр уведомлений

```
Актор:        Пользователь
Предусловие:  Есть уведомления
Триггер:      Клик на колокол или переход в /notifications
```

**Основной сценарий:**
1. GET /api/notifications → список уведомлений с пагинацией
2. Непрочитанные выделены визуально, счётчик на колоколе
3. Пользователь кликает на уведомление → редирект по `payload` (статья, набор, инвентарь)
4. POST /api/notifications/:id/read → отмечается прочитанным
5. Кнопка "Прочитать все" → POST /api/notifications/read-all

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Нет уведомлений | Пустое состояние "Всё тихо 🎉" |
| A2 | Фильтр по типу | Список фильтруется по category (инвентарь / контент / система) |

**Постусловие:** Прочитанные уведомления помечены, счётчик обнуляется

---

### UC-33 | Real-time доставка уведомлений

```
Актор:        Система (SSE) → Пользователь
Предусловие:  Пользователь авторизован и онлайн
Триггер:      Создание нового уведомления в БД
```

**Основной сценарий:**
1. При открытии приложения — установка SSE-соединения
2. При создании уведомления сервер пушит событие через SSE
3. Счётчик на колоколе обновляется без перезагрузки страницы
4. Тост-уведомление показывается поверх интерфейса

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | SSE не поддерживается / соединение потеряно | Fallback: polling GET /api/notifications/unread-count каждые 30 сек |

**Постусловие:** Пользователь видит уведомление в реальном времени

---

### UC-34 | Еженедельный дайджест

```
Актор:        Система (Scheduler) → Пользователь
Предусловие:  Уведомление `weekly_digest` включено в настройках
Триггер:      Каждое воскресенье в 18:00
```

**Основной сценарий:**
1. Scheduler формирует для каждого пользователя сводку за неделю:
   - Просроченные позиции инвентаря (`over`)
   - Заканчивающиеся позиции (`urgent`)
   - Новые официальные наборы SmartSpend
   - Новые статьи от авторов из подписок
2. Создаётся уведомление `weekly_digest` с этими данными
3. Пользователь получает уведомление в колоколе
4. Клик → сводная страница или детальное уведомление

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Нет изменений за неделю | Дайджест не создаётся (нечего показывать) |
| A2 | Дайджест отключён в настройках | Job пропускает пользователя |

**Постусловие:** Пользователь проинформирован о состоянии своего инвентаря и свежем контенте

---

### UC-35 | Уведомление о заморозке после добавления набора

```
Актор:        Система → Пользователь
Предусловие:  Пользователь добавил набор в конверт (UC-10)
Триггер:      Завершение POST /api/sets/:id/save
```

**Основной сценарий:**
1. Немедленно создаётся уведомление `inv_frozen`:
   "Добавлено 12 позиций в заморозку"
2. Пользователь видит уведомление в колоколе и тостер
3. Ежедневно (пока есть frozen позиции): напоминание "У вас N позиций в заморозке →"
4. При активации последней позиции: "Все позиции набора активированы ✅"

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Напоминание о заморозке уже отправлялось сегодня | Не дублировать, 1 раз в день |

**Постусловие:** Пользователь знает о замороженных позициях и мотивирован их активировать

---

## Блок 11 — Система (UC-36…37)

---

### UC-36 | Управление аккаунтом удалённого пользователя (Ghost)

```
Актор:        Система
Предусловие:  Пользователь выполнил UC-05 (удалил аккаунт)
Триггер:      Статус аккаунта изменился на `deleted`
```

**Основной сценарий:**
1. Немедленно: email и personal data анонимизируются
2. Все публикации (статьи, наборы) — `author_id` → ссылка на `ghost_user` (системный пользователь)
3. В ленте: "👻 Привидение" вместо имени автора
4. Клик по автору → GhostProfile: "Этот аккаунт был удалён. Контент остаётся доступным"
5. Через 30 дней: фоновый job выполняет полную очистку данных пользователя

**Постусловие:** Пользователь полностью удалён через 30 дней, контент сохранён

---

### UC-37 | Поиск по контенту

```
Актор:        Пользователь
Предусловие:  Пользователь авторизован (опционально для MVP)
Триггер:      Ввод запроса в поисковую строку
```

**Основной сценарий:**
1. GET /api/search?q=&type=sets|articles|users
2. Система ищет по:
   - Наборы: title, desc, items.name
   - Статьи: title, excerpt, body (первые 500 символов)
   - Пользователи: display_name, handle, bio
3. Результаты сгруппированы по типу
4. Клик на результат → соответствующая страница

**Альтернативные потоки:**

| ID | Условие | Результат |
|---|---|---|
| A1 | Запрос < 2 символов | Поиск не выполняется |
| A2 | Нет результатов | "Ничего не найдено по запросу «...»" |

**Постусловие:** —

---

## Сводная таблица Use Cases

| UC | Название | Актор | Модуль |
|---|---|---|---|
| UC-01 | Регистрация по email | Гость | Auth |
| UC-02 | Вход в аккаунт | Гость | Auth |
| UC-03 | Вход через OAuth | Гость | Auth |
| UC-04 | Сброс пароля | Гость | Auth |
| UC-05 | Удаление аккаунта | Пользователь | Auth |
| UC-06 | Редактирование профиля | Пользователь | Профиль |
| UC-07 | Изменение настроек | Пользователь | Профиль |
| UC-08 | Просмотр и фильтрация каталога | Пользователь | Каталог |
| UC-09 | Просмотр деталей набора | Пользователь | Каталог |
| UC-10 | Добавление набора в конверт | Пользователь | Каталог |
| UC-11 | Просмотр инвентаря | Пользователь | Инвентарь |
| UC-12 | Активация позиции из заморозки | Пользователь | Инвентарь |
| UC-13 | Пополнение расходника | Пользователь | Инвентарь |
| UC-14 | Ручное добавление позиции | Пользователь | Инвентарь |
| UC-15 | Архивация позиции | Пользователь | Инвентарь |
| UC-16 | Напоминание о заканчивающейся позиции | Система | Инвентарь |
| UC-17 | Заполнение финансовых данных | Пользователь | Финансы |
| UC-18 | Изменение ставки EmoSpend | Пользователь | Финансы |
| UC-19 | Автоматический пересчёт SmartBase | Система | Финансы |
| UC-20 | Просмотр ленты | Пользователь | Лента |
| UC-21 | Фильтрация ленты | Пользователь | Лента |
| UC-22 | Чтение статьи | Пользователь | Статьи |
| UC-23 | Написание и публикация статьи | Автор | Статьи |
| UC-24 | Редактирование статьи | Автор | Статьи |
| UC-25 | Комментирование статьи | Пользователь | Статьи |
| UC-26 | Анонимная публикация | Автор | Статьи/Наборы |
| UC-27 | Создание собственного набора | Автор | Наборы |
| UC-28 | Редактирование набора | Автор | Наборы |
| UC-29 | Подписка на автора | Пользователь | Социальное |
| UC-30 | Просмотр профиля автора | Пользователь | Социальное |
| UC-31 | Лайк набора или статьи | Пользователь | Социальное |
| UC-32 | Просмотр уведомлений | Пользователь | Уведомления |
| UC-33 | Real-time доставка уведомлений | Система | Уведомления |
| UC-34 | Еженедельный дайджест | Система | Уведомления |
| UC-35 | Уведомление о заморозке | Система | Уведомления |
| UC-36 | Ghost-аккаунт после удаления | Система | Система |
| UC-37 | Поиск по контенту | Пользователь | Система |
